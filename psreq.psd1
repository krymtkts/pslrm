@{
    InvokeBuild = @{
        Repository = 'PSGallery'
        Prerelease = $true
    }
    'Microsoft.PowerShell.PlatyPS' = @{
        Repository = 'PSGallery'
        Prerelease = $true
    }
    Pester = @{
        Repository = 'PSGallery'
        Prerelease = $true
    }
    PSKeepAChangelogTools = @{
        Repository = 'PSGallery'
    }
    PSScriptAnalyzer = @{
        Repository = 'PSGallery'
        Prerelease = $true
    }
}
